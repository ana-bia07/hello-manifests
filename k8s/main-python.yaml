apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-fastapi
  labels:
    app: app-fastapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-fastapi
  template:
    metadata:
      labels:
        app: app-fastapi
    spec:
      containers:
        - name: app-fastapi
          image: anabeatriz02/main-python:ca0f9a89e67368bdb6f3215c170e691ad2573451
          ports:
          - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: app-fastapi
  labels:
    app: app-fastapi
spec:
  type: NodePort
  selector:
    app: app-fastapi
  ports:
  - name: "app-fastapi-service"
    port: 8080
    targetPort: 80
    nodePort: 30000
    
